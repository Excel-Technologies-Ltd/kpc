name: Frappe Docker Build Dev KPC

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to deploy"
        required: true
        default: "master"
        type: string

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          repository: Excel-Technologies-Ltd/excel-frappe-docker

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Frappe Docker Image
        run: |
          export APPS_JSON='[
            {"url": "https://github.com/frappe/erpnext", "branch": "version-14"},
            {"url": "https://github.com/Excel-Technologies-Ltd/KPC.git", "branch": "master"},
            {"url": "https://gitlab.com/excel.azmin/whitelabel.git","branch": "main"},
            {"url": "https://github.com/excel-azmin/frappe_crewai.git", "branch": "master"}
          ]
          '

          export APPS_JSON_BASE64=$(echo ${APPS_JSON} | base64 -w 0)

          docker buildx build \
          --no-cache \
          --platform linux/amd64 \
          --build-arg FRAPPE_PATH=https://github.com/frappe/frappe \
          --build-arg FRAPPE_BRANCH=v14.64.0 \
          --build-arg PYTHON_VERSION=3.11.9 \
          --build-arg NODE_VERSION=22 \
          --build-arg APPS_JSON_BASE64="$APPS_JSON_BASE64" \
          --tag arcapps/kpc:latest \
          --file images/custom/Containerfile \
          --push .
